import { CustomStyleDirective } from './claims/custom-style.directive';

describe('CustomStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStyleDirective();
    expect(directive).toBeTruthy();
  });
});
